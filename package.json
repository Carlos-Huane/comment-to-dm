{
  "name": "comment-to-dm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Comment-to-DM bot para Instagram usando Graph API directa (alternativa propia a ManyChat)",
  "scripts": {
    "start": "node src/index.mjs",
    "dev": "node --watch src/index.mjs"
  },
  "dependencies": {
    "express": "^4.21.1",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=20"
  }
}
