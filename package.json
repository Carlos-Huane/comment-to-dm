{
  "name": "comment-to-dm",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Comment-to-DM bot para Instagram usando Graph API directa (deploy Vercel serverless)",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": "24.x"
  }
}
